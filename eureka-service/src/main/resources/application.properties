spring.application.name=eureka-service
server.port=8761

# This app IS the registry, so it doesn't need to register with
# itself or look itself up. Disabling this avoids noisy (but
# harmless) error logs on startup.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false